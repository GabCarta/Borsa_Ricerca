id: prova
Profile:
  id: prova
  OS: Android
database:
  host: mio-mongo
  port: 27017
  db_name: DB_generico
  dbname: DB_generico
collections:
  cond:
    db_collection_name: cond
    required_fields:
      id: string
    allowed_id:
    - cond
    allowed_values:
      stato:
      - 'ON'
      - 'OFF'
      consumo: float
brokers:
  mqtt:
    broker_address: broker.mqttdashboard.com
    port: 1883
    topic_subscribe: device_carta
